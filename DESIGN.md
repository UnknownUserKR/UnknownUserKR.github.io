# DESIGN.md — 권민재 포트폴리오

이 사이트의 디자인 시스템 문서. 코딩 에이전트(Claude Code · Codex · Cursor 등)가 이 저장소에서
UI 작업을 할 때 **먼저 읽고 따라야 하는 기준**이다. 새 색·새 폰트·새 간격을 발명하지 말고
아래 토큰을 쓴다.

- 최종 갱신 2026-08-26 (안티슬로프 리디자인 — 벤치마킹 근거는 `../docs/2026-08-26-포트폴리오-리디자인-벤치마킹.md`)
- 대상 `index.html` · `ai.html` · `bio.html` (빌드 도구 없는 순수 정적 HTML, 각 파일에 `<style>` 인라인)
- 배포 GitHub Pages — https://unknownuserkr.github.io/

---

## 핵심 원칙 — 두 개의 목소리

이 사이트는 **의도적으로 두 가지 시각 언어**를 쓴다. 하나로 통일하지 말 것.

| | 지면(Voice A) | 계측판(Voice B) |
|---|---|---|
| 페이지 | `index.html` | `ai.html` · `bio.html` |
| 역할 | 사람을 소개하는 개인 홈 | 근거를 제시하는 포트폴리오 |
| 바탕 | 따뜻한 종이 `#F8F5EF` | 차가운 계기판 `#e9edee` |
| 표제 | 세리프 (Noto Serif KR 900) | 산세리프 (Pretendard 700) |
| 강조 | 이끼 초록 `#3E6B4F` | 청록 `#0c7687` |
| 밀도 | 여백 넓게, 읽는 글 | 조밀하게, 훑는 데이터 |

**왜** — 홈은 "어떤 사람인가"를 읽히는 곳이고, 포트폴리오는 "무엇을 했나"를 검증받는 곳이다.
읽는 문서와 훑는 계기판은 같은 옷을 입으면 안 된다.

---

## 토큰 — Voice A (index.html)

```css
--paper:   #F8F5EF;   /* 지면 */
--ink:     #211D18;   /* 본문        15.39:1 */
--sub:     #6C6257;   /* 보조 본문    5.48:1 */
--faint:   #7B6F5F;   /* 캡션·연도    4.51:1 */
--rule:    #E4DCCE;   /* 장식 괘선 (텍스트 아님) */
--accent:  #3E6B4F;   /* 강조·링크    5.64:1 */
--accent-soft: #E9EFE8;

--serif: "Noto Serif KR", "Apple SD Gothic Neo", serif;
--sans:  "Pretendard Variable", Pretendard, -apple-system, …;
--mono:  "IBM Plex Mono", ui-monospace, …;

--measure: 42rem;     /* 본문 최대 폭 */
```

## 토큰 — Voice B (ai.html · bio.html)

두 파일은 **완전히 동일한 토큰 블록**을 쓴다. 한쪽을 고치면 반드시 다른 쪽도 고친다.

```css
--ground:   #e9edee;  --surface: #ffffff;
--surface-2:#f3f6f7;  --surface-3:#e7ecee;
--ink:      #0b1a24;  /* 본문        15.00:1 */
--ink-2:    #415765;  /* 보조         6.42:1 */
--ink-3:    #5a6e79;  /* 캡션·라벨    4.52:1 */
--rule:     #d2dbde;  --rule-2: #b5c3c8;
--trace:    #0c7687;  /* 강조         4.50:1 */
--trace-soft:#dceff3;
--critical: #c2372f;  --warn: #8f6305;   /* 4.59 · 4.50 */

--t-xs:.6875rem  --t-sm:.8125rem  --t-base:.9375rem
--t-md:1.0625rem --t-lg:1.3125rem --t-xl:1.875rem
--page:78rem  --measure:72ch  --r:3px
```

`--shadow` · `--glow` · `--trace-dim`은 2026-08-26에 **삭제**했다(네온 글로우·카드 그림자 폐지, dim은 trace와 동일값이었음). 되살리지 말 것.

---

## 컴포넌트 보이스 (2026-08-26 리디자인 — 안티슬로프 규칙)

베스트 포트폴리오 스터디(brittanychiang · leerob · rauno · p5aholic · jbee · junghyeonsu) 후
"부트캠프 템플릿"과 갈라서기 위해 정한 규칙. **되돌리면 다시 슬로프가 된다.**

**Voice A (index)**
- **모노·대문자 킥커 금지.** 섹션은 세리프 표제 하나로 선다 (leerob 방식). 모노는 이제 index에서 쓰지 않는다.
- Selected Work는 **번호 없는 헤어라인 행** — 제목(세리프) + 지표 한 줄 + 방향 라벨. 호버는 배경·색·arrow transform만 (레이아웃 속성 금지).
- 두 갈래 도어는 **카드가 아니라 상단 2px 잉크 룰 블록.** 링크 라벨은 raw URL이 아니라 행동 문장("AI 포트폴리오 보기 →").
- 상태줄(`.status`)은 점·대문자 없는 조용한 한 줄.

**Voice B (ai · bio)**
- **프로젝트는 카드 그리드가 아니라 전폭 행 인덱스.** `날짜+분류(모노, 좌) | 제목+소속+스택(중) | 지표(모노, 우)` 3열, 모바일은 세로 스택. `.card` 클래스명은 JS 훅이라 유지한다.
- **칩·필·태그 박스 금지.** 필터는 텍스트 + 위첨자 카운트(`.chip .n`, junghyeonsu 방식), 스택은 `·` 구분 텍스트, 분류는 색 없는 모노 텍스트. 카테고리 색 부호화(5색) 폐지 — 악센트는 `--trace` 하나.
- **KO·EN 쌍 리스트의 구분점**은 `html[lang] … ~ …::before` 패턴으로 넣는다(`:last-child` 기반은 쌍 때문에 깨짐). 스택은 flex-wrap 아이템이라 구분점이 항목과 함께 줄바꿈된다.
- **readout 스트립이 유일한 패널.** 나머지는 전부 지면 위 괘선 문서다. Skills도 빈 셀 없는 정의 행.
- 글로우·펄스·카운트업·미터 바 금지. 남은 모션은 상태 전환뿐.

**공통**
- `word-break: keep-all`은 **body에** 건다 (`html[lang="en"] body`가 normal로 복귀). 특정 요소에만 걸어서 표제가 "다룹니/다"로 끊긴 사고가 2026-08-26 이전까지 실존했다.
- 무한 애니메이션 금지 (구 `.dot` pulse 폐지).
- 한국어 모노 라벨에 대문자용 자간(.1em+)을 걸지 않는다 — `[data-lang="ko"]` 스코프로 .02~.06em.

---

## 접근성 하한선 (타협 불가)

- **모든 본문·캡션 색은 자기 배경에서 4.5:1 이상.** 위 토큰은 전부 검증됨.
  새 색을 넣으면 반드시 대비를 계산하고 이 표에 기록한다.
- `--rule` 계열은 **장식 괘선 전용**. 텍스트나 의미 있는 UI 경계에 쓰지 않는다.
- 모든 초점 이동에 `:focus-visible` 링을 보인다. 링 등장에 애니메이션을 걸지 않는다.
- `prefers-reduced-motion: reduce`에서 모든 등장 모션을 끈다 (이미 구현됨).
- 각 페이지 첫 요소는 본문 건너뛰기 링크(`.skip`).

## 한국어 타이포그래피

- **줄바꿈은 어절 단위** — `word-break: keep-all`이 세 페이지 모두 **body에** 걸려 있다
  (`html[lang="en"] body { word-break: normal }`). 기본값은 글자 중간에서 끊어져
  "유전체 데이 / 터 분석"처럼 보인다.
- 표제에 `text-wrap: balance`.
- **표제에 이탤릭 금지.** 강조는 굵기·색으로. `em`은 `font-style: normal`로 재정의해 쓴다
  (이미 `.chapter p em`, `.lede em`에 적용됨).

## 반응형 하한선 — 320px

가장 좁은 기기(320px)에서 **가로 스크롤이 생기면 버그**다. 세 페이지 모두 320 / 375 / 414 / 768에서
`document.scrollWidth === window.innerWidth`를 만족한다. 이걸 깨는 대표적 실수 두 가지:

- **그리드 트랙이 컨테이너보다 넓은 경우.**
  `repeat(auto-fill, minmax(19rem, 1fr))`는 컨테이너가 19rem보다 좁으면 그대로 넘친다.
  반드시 `minmax(min(19rem, 100%), 1fr)`로 쓴다. 이 저장소의 9곳 전부 적용돼 있다.
- **플렉스 자식이 압축돼 글자가 깨지는 경우.**
  버튼·칩처럼 짧은 라벨을 가진 요소에는 `flex: none; white-space: nowrap`을 준다.
  없으면 `.langswitch`의 "EN"이 두 글자에서 줄바꿈되고 부모의 `overflow: hidden`에 잘린다.

`html, body { overflow-x: clip }`는 안전망이지 해결책이 아니다. **넘치는 원인을 먼저 없앤다** —
clip은 증상을 가리므로 `scrollWidth` 측정만으로는 버그를 놓친다. 요소별 `getBoundingClientRect().right`로 확인할 것.

## 모션

- 상태 전환 `.18s`~`.2s`. 유일한 등장 모션은 index의 곡선 드로잉(1.6s, 최초 노출 1회)뿐이다.
  2026-08-26에 섹션 fade-up(`.rise`) · 게이지 카운트업 · 미터 바 채우기 · 상태점 펄스를 전부 제거했다.
- 이징은 `cubic-bezier(.3,0,.2,1)` 계열로 통일. 브라우저 기본 `ease`와 바운스는 쓰지 않는다.
- `transform`과 `opacity`만 애니메이트한다. 레이아웃 속성은 건드리지 않는다 (구 `.works a:hover`의
  padding 애니메이션도 이 규칙 위반이라 제거됨).
- **모션은 더하기 전에 뺀다.** 없애도 정보가 사라지지 않으면 없앤다.

## 카피

- 수치는 **실측값만.** 없는 지표를 만들지 않는다. 확인 안 된 값은 비워두거나 다른 구성으로 바꾼다.
- 한국어·영어 병기는 `data-lang` 속성 + `html[lang]` 토글. 한쪽만 고치지 않는다.
- 링크·버튼 문구는 결과를 그대로 말한다.

---

## index 구성 요소 (Voice A)

페이지 순서: 상단바 → 히어로 → 곡선 → **한눈에(`#glance`)** → Story → Selected Work → 두 갈래 → Now·연락 → **footnav** → 콜로폰.
섹션 킥커(FACTS/STORY/…)는 2026-08-26에 제거했다 — 세리프 표제가 단독으로 선다.

- `.facts` — "정보 한눈에" 정의 목록. 학력·교육·기록·도구·데이터·수료 6행.
  **여기 들어가는 내용은 전부 ai/bio 페이지나 이력서에서 검증 가능한 사실이어야 한다** — 수치·명칭을 새로 만들지 않는다.
  셀 상단 헤어라인(`--rule`) + 모노 라벨(`--faint`) + 본문(`--sub`)의 조용한 구성 유지.
- `.footnav` — 기능하는 푸터 내비 (AI·Bio·GitHub·LinkedIn·Email·`#top`). Now 섹션에는 내비를 두지 않는다
  (과거 `.outlinks`는 footnav와 중복이라 제거함, 2026-08-24). 연락 CTA는 `.contact-big` 이메일 하나로 충분하다.
- 벤치마킹 근거와 적용/보류 판정은 `../docs/2026-08-24-개인사이트-벤치마킹.md`.

## 반복되는 모티프

**용량–반응 곡선.** `index.html`의 시그모이드 SVG는 장식이 아니라 이 사람의 논지다 —
"용량–반응 곡선과 학습 곡선을 같은 눈으로 읽는다". OG 카드에도 같은 곡선이 들어간다.
다른 곳에 쓸 때도 의미가 맞을 때만 쓴다.

## 공유 카드 (OG 이미지)

`assets/og/home.png` · `ai.png` · `bio.png` — 1200×630.
소스는 저장소에 없다(스크래치패드에서 headless Chrome으로 렌더). 다시 만들 때는
각 페이지의 토큰을 그대로 쓰고, **표기하는 수치는 반드시 페이지에 있는 실측값과 일치**시킨다.

> 알려진 불일치 — `home.png`의 표제는 사이트의 명조가 아니라 고딕으로 렌더됐다.
> headless 렌더러가 웹폰트를 적용하지 못했고, 썸네일 크기에서는 고딕 가독성이 더 낫다고 판단해 두었다.
> 정확한 일치가 필요하면 명조를 로컬 설치한 환경에서 재렌더할 것.

---

## 작업 규칙

1. **수정 전** `cd site && git fetch && git status` — 원격이 앞서 있을 수 있다.
2. 사실(숫자·날짜·역할)을 고치면 **4곳을 함께** 갱신한다 — 이력서 2벌 + 사이트 2버전.
   이력서는 `scripts/make_resumes.py`로 생성하며 docx를 직접 고치지 않는다.
3. `portfolio.html` 류의 사본을 만들지 않는다. 원본은 `site/`의 3개 파일뿐이다.
4. 새 토큰을 넣으면 이 문서에 기록한다. 기록되지 않은 값은 다음 에이전트가 지운다.
